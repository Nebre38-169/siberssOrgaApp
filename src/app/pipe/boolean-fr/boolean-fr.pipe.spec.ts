import { BooleanFrPipe } from './boolean-fr.pipe';

describe('BooleanFrPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanFrPipe();
    expect(pipe).toBeTruthy();
  });
});
